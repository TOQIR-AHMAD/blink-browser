add_subdirectory(unit)

# Added in later phases:
#   integration/  browser start-up, navigation, tab lifecycle (Phase 4)
#   privacy/      disk-persistence, cookie, history and cleanup checks (Phase 8)
#   ui/           QML tests (Phase 9)
